"=========== Meta ============
"StrID : 
"Title : 数据库笔试题
"Slug  : 
"Cats  : Uncategorized
"Tags  : 数据库,笔试
"=============================
"EditType   : post
"EditFormat : Markdown
"BlogAddr   : http://www.fangze.info
"========== Content ==========
 
[数据库笔试题及答案](http://wenku.baidu.com/view/9229fa2458fb770bf78a5562.html)
**有一些Visual Fox Pro的内容，可以忽略之**

下面描述中，符合结构化程序设计风格的是(A)
Q：选项D呢？
A.使用顺序、选择和重复(循环)三种基本控制结构表示程序的控制逻辑
B.模块只有一个入口，可以有多个出口
C.注重提高程序的执行效率
D.不使用goto语句
在结构化方法中，用数据流程图(DFD)作为描述工具的软件开发阶段是(B)
A.可行性分析
B.需求分析
C.详细设计
D.程序编码
数据库系统的核心是(B)
A.数据模型
B.数据库管理系统
C.软件工具
D.数据库
下列叙述中正确的是(C)
Q：A和D肯定不对，但是B呢？
A.数据库是一个独立的系统，不需要操作系统的支持
B.数据库设计是指设计数据库管理系统
C.数据库技术的根本目标是要解决数据共享的问题
D.数据库系统中，数据的物理结构必须与逻辑结构一致
如果要创建一个数据组分组报表，第一个分组表达式是“部门”，第二个分组表达式是“性别”，第三个分组表达式是“基本工资”，当前索引的索引表达式应当是(B)
Q：完全不懂题意
A.部门+性别+基本工资
B.部门+性别+STR(基本工资)
C.STR(基本工资)+性别+部门
D.性别+部门+STR(基本工资)
把一个项目编译成一个应用程序时，下面的叙述正确的是(A)
Q：我选的是D。。。
A.所有的项目文件将组合成一个单一的应用程序文件
B.所有项目的包含文件将组合成一个单一的应用程序文件
C.所有项目排除的文件将组合成一个单一的应用程序文件
D.由用户选定的项目文件将组合成一个单一的应用程序文件
要控制两个表中数据的完整性和一致性可以设置“参照完整性”，要求这两个表(A)
Q：什么是自由表？
A.是同一个数据库中的两个表
B.不同数据库中的两个表
C.两个自由表
D.一个是数据库表另一个是自由表
定位第一条记录上的命令是(A)
A.GO TOP
B.GO BOTTOM
C.GO 6
D.SKIP
在关系模型中，实现“关系中不允许出现相同的元组”的约束是通过(B)
A.候选键
B.主键
C.外键
D.超键
设当前数据库有10条记录(记录未进行任何索引)，在下列三种情况下，当前记录号为1时；EOF()为真时；BOF()为真时，命令?RECN()的结果分别是(A)
Q：RECN()是什么命令？EOF()和BOF()又是什么命令？
A：RECNO()是测试记录号的函数。文件头(BOF)的记录号为1。文件尾(EOF)的记录号是最后一条记录的记录号+1。SQL函数查询：[W3school](http://www.w3school.com.cn/sql/sql_functions.asp)
A.1,11,1
B.1,10,1
C.1,11,0
D.1,10,0
下列表达式中结果不是日期型的是(C)
Q：每句分别是什么意思？
A.CTOD("2000/10/01")
B.{^99/10/01}+365
C.VAL("2000/10/01")
D.DATE()
索引字段值不唯一，应该选择的索引类型为(B)
Q：各选项分别是什么？
A.主索引
B.普通索引
C.候选索引
D.唯一索引
执行SELECT 0选择工作区的结果是(B)
A.选择了0号工作区
B.选择了空闲的最小号工作区
C.关闭选择的工作区
D.选择已打开的工作区
DELETE FROM S WHERE 年龄>60语句的功能是(B)
A.从S表中彻底删除年龄大于60岁的记录
B.S表中年龄大于60岁的记录被加上删除标记
C.删除S表
D.删除S表的年龄列
在SQL中，删除视图用(C)
Q：记错了。A是删除模式。
A.DROP SCHEMA命令
B.CREATE TABLE命令
C.DROP VIEW命令
D.DROP INDEX命令
以下属于非容器控件的是(B)
Q：不懂。。。
A.Form
B.Label
C.page
D.Container
将查询结果放在数组中应使用(D)短语
Q：其实是可以猜出来的。。。
A.INTO CURSOR
B.TO ARRAY
C.INTO TABLE
D.INTO ARRAY
在命令窗口执行SQL命令时，若命令要占用多行，续行符是(D)
Q：额，猜不出来。。。
A.冒号(:)
B.分号(;)
C.逗号(,)
D.连字符(-)
数据的逻辑结构在计算机存储空间中的存放形式称为数据的(模式/逻辑模式)。
关系数据库管理系统能实现的专门关系运算符包括选择、连接和(投影)。
命令?LEN("THIS IS MY BOOK")的结果是(15)。
**这些命令是哪里用到的？怎么用？去实际操作之**

SQL SELECT语句为了将查询结果存放到临时表中应用使用(Into cursor/Into cursor cursorname)短语。
多栏报表的栏目数可以通过(页面设置/列数)来设置。
在打开项目管理器之后再打开“应用程序生成器”，可以通过按ALT+F2键，快捷菜单和“工具”菜单中的(应用程序生成器)。
**这是哪个软件的？**

查询设计器中的“联接”选项卡，可以控制(联接类型)选择。

**学习各种SQL命令，命令都有缩写形式，亦学习之**


结构化程序设计主要强调的是(B)
A.程序的规模
B.程序的易读性
C.程序的执行效率
D.程序的可移植性
在软件生命周期中，能准确地确定软件系统必须做什么和必须具备哪些功能的阶段是(D)
A.概要设计
B.详细设计
C.可行性分析
D.需求分析
视图设计一般有3种设计次序，下列不属于视图设计的是(B)
A.自顶向下
B.由外向内
C.由内向外
D.自底向上
下列说法中，不属于数据模型所描述的内容的是(C)
数据模型应当描述数据对象的三个要素：数据组织结构、数据操作和数据的完整性约束。
A.数据结构
B.数据操作
C.数据查询
D.数据约束
在下面的Visual FoxPro表达式中，运算结果是逻辑真的是(D)
A.EMPTY(.NULL.)
B.LIKE('acd','ac?')
C.AT('a','123abc')
D.EMPTY(SPACE(2))
表达式VAL(SUBS("奔腾586",5,1))*Len("visual foxpro")的结果是(D)
中文占2个字节，所以第5个是数字5。
5*13=65
A.13.00
B.14.00
C.45.00
D.65.00
以下关于自由表的叙述，正确的是(C)
A.全部是用以前版本的FOXPRO(FOXBASE)建立的表
B.可以用Visual FoxPro建立，但是不能把它添加到数据库中
C.自由表可以添加到数据库中，数据库表也可以从数据库中移出成为自由表
D.自由表可以添加到数据库中，但数据库表不可从数据库中移出成为自由表
下面关于数据环境和数据环境中两个表之间的关系的叙述中，(C)是正确的。
A.数据环境是对象，关系不是对象
B.数据环境不是对象，关系是对象
C.数据环境是对象，关系是数据环境中的对象
D.数据环境和关系均不是对象
设有关系R1和R2，经过关系运算得到结果S，则S是(A)
A.一个关系
B.一个表单
C.一个数据库
D.一个数组
DBAS指的是(C)
A.数据库管理系统
B.数据库系统
C.数据库应用系统
D.数据库服务系统
设X="ABC",Y="ABCD",则下列表达式中值为.T.的是(C)
A.X=Y
B.X==Y
C.X$Y
D.AT(X,Y)=0
在表结构中，逻辑型、日期型、备注型字段的宽度分别固定为(D)。
A.3，8，10
B.1，6，4
C.1，8，任意
D.1，8，4
查询订购单号(字符型，长度为4)尾字符是"1"的错误命令是(C)。
A.SELECT * FROM 订单 WHERE SUBSTR(订购单号,4)="1"
B.SELECT * FROM 订单 WHERE SUBSTR(订购单号,4,1)="1"
C.SELECT * FROM 订单 WHERE "1"$订购单号
D.SELECT * FROM 订单 WHERE RIGHT(订购单号,1)="1"
根据“职工”项目文件生成emp_sys.exe应用程序的命令是(B)。
A.BUILD EXE emp_sys FROM 职工
B.BUILD APP emp_sys.exe FROM 职工
C.LIKE EXE emp_sys FROM 职工
D.LIKE APP emp_sys.exe FROM 职工

数据库系统的三级模式分别为(概念/概念级)模式、内部级模式与外部级模式。
关系模型的数据操纵即是建立在关系上的数据操纵，一般有(查询)、增加、删除和修改四种操作。
(内部联接)是指只有满足联接条件的记录才包含在查询结果中。
设有如下关系表R、S和T：
R(BH,XM,XB,DWH)
S(SWH,DWM)
T(BH,XM,XB,DWH)
实现RUT的SQL语句是((SELECT * FROM R) UNION (SELECT * FROM T))。
